# GROUP 3
## Phishing URL (Website) Detection

You can run the code locally or via Google Colab. In the former case, you must have the following installed and can run via the play icons/buttons in the UI. 
In the latter case (highly suggested), you can select any CPU runtime for A2/A3. For A4 and beyond, optionally select a GPU runtime like T4 for a faster output. 
- pytorch
- numpy
- pandas
- matplotlib
- seaborn
- jupyter notebook (or get the jupyter extension in VS code)
